## 1.0.0

* First release of `brilliant-sdk` — a meta-package that installs both `brilliant-ble` and `brilliant-msg`
* Supports Brilliant Labs Frame and Halo devices
* Replaces `uv add frame-ble frame-msg` with a single `uv add brilliant-sdk`
